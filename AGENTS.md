# AGENTS.md

AIコーディングツールと、それを使う人のための実装規約。

## ルールの置き場所

ルールの種類ごとに、正とする文書は1つだけにする。他の文書は正とする文書を参照し、中身を書き写さない。食い違ったら、正とする文書が優先し、もう一方はバグとして直す。

| ルールの種類 | 正とする文書 |
|---|---|
| セキュリティとドメインの不変条件、およびその中身を決める一覧 | [`docs/architecture/invariants.md`](docs/architecture/invariants.md) |
| 設計判断の記録 | [`docs/adr/`](docs/adr/README.md) |
| 用語の意味・使う識別子 | [`docs/domain/glossary.md`](docs/domain/glossary.md) |
| 要件と設計の背景、モジュールの構成 | [`docs/requirements.md`](docs/requirements.md) |
| プロセス：役割、レビュー、停止条件、高リスクの範囲、CI、ruleset | [`docs/process/workflow.md`](docs/process/workflow.md) |
| 実装規約、書き方 | **このファイル** |

**記録済みの決定をPRの中で決め直さない。** 変えたいときはIssueを立てる。

## 絶対にしないこと（要約）

見落とすと危険な不変条件の要約である。**正は不変条件の各行**にあり、ここに書いていない条件も守る。

- テナントの境界を越えない。テナント所有データへのクエリは `db.TenantTx` の中で、`tenant_id` で絞って実行する（I-1〜I-3、I-34）
- 業務データから `User` を参照しない（I-4）
- テナントはURLから決める（I-5）
- 認可はサーバーで行い、すべてのユースケースで Actor を受け取って認可を通す。認可を省く手段を作らない（I-6）
- 認可を決めるコードは `internal/authz/**` にだけ置く（I-42）
- `priest` 段階には「自分の担当」以外を見せない（I-7）
- 変更履歴・画面への通知で認可を迂回させない（I-35、I-36）
- ハンドラから System Actor を作らない（I-37）
- ログ・ジョブの引数に個人情報を入れない（I-19、I-20）
- 実データ・秘密情報・実在の名称を書かない（I-21）
- 脆弱性の情報を公開の場に書かない（I-33）
- ハンドラ・ジョブのワーカー・CLI から DB を直接呼ばない。ユースケースを呼ぶ（I-23）
- テスト・lint・静的解析を弱めて通さない。赤いチェックは、そうでないと示されるまではコードが間違っている（I-26）
- PRを承認しない（I-32）

## 作業ルール

- **Issueが求めることをし、それ以上をしない。** 他に直したいものは別のIssueにする
- **振る舞いの変更とリファクタリングは別のPRにする。**
- **実装が1つしかないのに、インターフェースや抽象層を導入しない。** インターフェースは使う側で、必要になったときに定義する。テスト用の偽物も実装の1つに数える
- **依存を追加するときは理由を書く。** それなしでは何ができないか、代わりに何を検討したか、ライセンスは何か。Issueにない依存の追加は[停止条件](docs/process/workflow.md#停止条件)である
- **外部ライブラリ・ツールの挙動に自信がなければ、記憶ではなく公式ドキュメントと、使っている版のソースで確かめる。**
- **停止条件に当たったら止まる。** 停止条件の一覧は [workflow.md](docs/process/workflow.md#停止条件) にだけある

## Go の書き方

- パッケージの構成は[要件定義 §3.2](docs/requirements.md#32-アーキテクチャ方針)に従う。モジュールの実装は `internal/<モジュール>/internal/**` に置き、他のモジュールには公開パッケージの関数だけを見せる
- 識別子は用語集の英語名を使う。パッケージ名は短い小文字の単数形にし、`matter.MatterService` のように名前を重ねない
- 想定される失敗は `error` で返す。`panic` はバグと起動時の前提条件だけに使う。エラーは `fmt.Errorf("…: %w", err)` で文脈を付けて包み、握りつぶさない。呼び出し側が分岐するエラーは、パッケージの公開した値か型にし、`errors.Is` / `errors.As` で調べる
- `context.Context` は最初の引数で受け取り、構造体に保存しない
- DBへのアクセスは sqlc で生成したコードで行う。SQLは各モジュールの `internal/store` に置く。テナント所有データへのクエリは `db.TenantTx` の中でだけ実行する
- 時刻は時計のインターフェース経由で取り、UTCで保存する。年や日付の境界は Asia/Tokyo で判定する
- ログは `log/slog` で出す。個人情報を持つ値は専用の型にし、ログでは伏せる（I-19）
- goroutine を起動した側が、その終了まで責任を持つ。リクエストの外で続く処理は goroutine ではなくジョブ（River）にする
- 列挙（ステータスなど）は型を定義し、`switch` は exhaustive で網羅を検査する

## 「動く」と言う前に

```bash
make check
```

CIの必須チェックのうちローカルで実行できるものをまとめて実行する。チェックの一覧は [workflow.md](docs/process/workflow.md#ci) にある。

## テスト

- DBに触れるものは**本物のPostgreSQL**で、**アプリ用ロール**の接続でテストする。DBのモックは使わない（I-2）
- **テナントに属する機能には、必ず別テナントからの否定テストを付ける。** 何も見えないことを確認する
- 権限に関わる機能には、権限の段階（`admin` / `office` / `priest`）と「Priestに紐付いているか」の組み合わせのテストを付ける
- ルートは種類付きで1つのルート表に登録し、[種類ごとの否定テスト](docs/architecture/invariants.md#ルートの種類と否定テスト)を表駆動で網羅する（I-5）
- 偽物（fake）は外部システム（メール等）にだけ使う
- 正規化（かな・全角半角・電話番号）、案件番号の採番、テナント分離には、プロパティテスト（rapid）も付ける。正規化にはファジングも付ける
- テストは表駆動を基本にし、`-race` で実行する。**並行性のテストは本当に並行に実行する**
- 何も検証していないテスト（アサーションがない、必ず成功する）を書かない

## 画面（templ + htmx）

- 画面の表示制御は補助であり、認可の判断はサーバーが行う（I-6）。許可されていない項目はHTMLに含めない
- ハンドラに業務ロジックを書かない。ユースケースを呼び、結果を templ で描画する
- 読み取りは GET、変更は POST で行い、変更のリクエストには必ず CSRF トークンを付ける
- htmx の部分更新の応答は、置き換える部分のHTMLだけを返す
- 画面の文言は日本語で書く（多言語化しない）。用語は用語集の表示名に合わせる
- セマンティックなHTMLを基本にする
- テストは `httptest` とHTMLの検証で書く。ブラウザが必要な導線だけブラウザのテストを使う

## 書き方

- コメント・ドキュメント・コミットメッセージ・Issue・PRの文章は**日本語**で書く。コードの識別子は英語で、用語集の識別子を使う
- **コメントには「なぜ」を書き、「何を」は書かない。**
- PRタイトルとコミットメッセージは [Conventional Commits](https://www.conventionalcommits.org/) に従う。型は英語、本文は日本語（例：`feat: 案件番号の年単位採番を追加`）。PRはsquash mergeするので、タイトルが `main` のコミットになる
- ブランチは `issue-<番号>-<短い英語のslug>` とし、`main` では作業しない
